'use strict';
// preview/site/static/js/libs/anime.min.js — anime.js のローカルスタブ(fixture 所有)。
// 実ライブラリは quantz-web の git に含まれない(MANIFEST 参照)。抽出JSに anime() の
// 消費は無く、common.html の読み込みを 404 にしないための空実装のみ置く。
function anime() { return { finished: Promise.resolve() }; }
anime.timeline = function () { return { add() { return this; } }; };
