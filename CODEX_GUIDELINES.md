# CODEX_GUIDELINES.md — Codexに対するオーナー指示

Codexとの作業中に受けたオーナー指示を、Claude Code用の `docs/GUIDELINES.md` と分離して記録する。
Codexは新しい依頼のたびに本書を参照する。

Rules
- オーナーの指示は原文のまま記録する。
- オーナーが良い例・悪い例を示した場合は、その実例を記録する。
- Codexによる一般化や解釈を、オーナー指示として書き加えない。
- 各記録には番号を付ける。

---

## 1. 説明は短すぎず長すぎず、理由と起きることが自然に分かるようにする

### オーナー指示原文

> 「もう少しちゃんと説明してくれないかな。これをしますだけだとよくわからない。」
>
> 「具体的に何を書き込んで、どのファイルに何を書こうとしているのか、それを見てからにした方がいい。ただし、長々と説明を大量にするのはやめろよ。必要十分な説明だけをしてくれ。」
>
> 「その説明は、これから実行することを説明するのにしては長すぎるから、もう少し必要十分にまとめろよ。さっきまでは短すぎるが今は長すぎる。」
>
> 「これをしますじゃなくて、こういう理由でこうしますとか、こうするとこうなりますとか、理由とかそれによって何が起きるかをちゃんと合わせて説明しろ。」
>
> 「なんか読みづらいというか、直感的な流れで説明できてないんだよ。こうだからこうして、そしてこうなるっていう、そういう順序で完全に言えないんだ。」
>
> 「『操作・理由・結果を因果順で説明する』これはお前の解釈であって、俺はそんなことを言っていないだろう。そういう勝手な指示を勝手に書き込むと、他の人が見た時に、それの通りにやろうとしておかしくなるだろう。俺の指示原文とよい例悪い例だけ書いておけばいいんだよ。」

### 良い例

> 今は `.env` がmonorepo内の `k00bot2/` に残っています。このままmonorepoからk00bot2を外すと `.env` まで失うため、まず独立cloneの `~/Sources/k00bot2/.env` へ移します。
>
> 秘密情報を安全な場所へ移したら、k00bot2をmonorepoへ追加したmerge commit `cafbc06` をrevertします。これにより履歴を壊さず、monorepoのファイル構成だけをk00bot2追加前へ戻せます。
>
> 次に、戻した結果を追加直前のcommit `87491fc` と比較します。完全に一致すれば、infraなど他の部分を巻き込まず、k00bot2だけを正しく外せたことになります。
>
> そこまで確認できた場合に限りGitHubへpushします。結果として、monorepoは元の構成へ戻り、k00bot2と `.env` は独立した `~/Sources/k00bot2` 側に残ります。EC2はまだ変更しません。

### 悪い例: 短すぎる

> 今回行うのは次の4点です。
>
> - `.env` を独立cloneへ移す。
> - merge commitをrevertする。
> - merge直前と一致することを検証する。
> - 通常pushする。

### 悪い例: 長すぎる

> ### 1. `.env` を独立リポジトリへ移動
>
> 実行する内容:
>
> 移動元: `~/Sources/thinkx-system/k00bot2/.env`
>
> 移動先: `~/Sources/k00bot2/.env`
>
> これはAPIキーの内容を読んだり変更したりせず、ファイルをそのまま移動します。
>
> 実行後:
>
> `~/Sources/thinkx-system/k00bot2/.env` 存在しない
>
> `~/Sources/k00bot2/.env` 存在する
>
> これにより、monorepoから `k00bot2/` を取り除いても `.env` が失われません。
>
> ### 2. monorepo統合commitをrevert
>
> 対象commit: `cafbc06 merge: integrate k00bot2 into monorepo`
>
> `git revert -m 1 cafbc06` を実行します。これはcommit履歴を消したり、ブランチを過去へ戻したりする操作ではありません。

## 2. 名前を見ただけで用途が分かるようにする

### オーナー指示原文

> 「名前を見ただけでそれが一体何なのかが一瞬で分からなければいけない。」

### 良い例

`style_class_inventory/simplicity_legacy_css_classes.txt`

### 悪い例

`test/inventory/classes_legacy.txt`

## 3. 重要な文書は発見しやすい場所に置く

### オーナー指示原文

> 「重要なものが奥の方に入っていたら見つけるのが難しいだろう。なるべくトップに近い、ほぼトップか、またはドックスの中に入っているべきじゃないのか。」

### 良い例

`docs/STYLE_CLASS_INVENTORY.md`、`docs/SITE_FIXTURE.md`

### 悪い例

`test/inventory/README.md`、`test/inventory/site_fixture_plan.md`
