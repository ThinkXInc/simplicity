# simplicity リファクタリング作戦規律(CLAUDE_REFACTORING.md)

**寿命: リファクタリング作戦の期間中のみ。** 完了後は docs/archive/ へ格納し、長期規約
(CLAUDE.md)だけが残る。本ファイルはワークスペース共通規律(monorepo 側 CLAUDE.md)を
simplicity 単独セッションの文脈に移植したものである。

## 実行単位の規律(最重要)

- **1セッション = 1計画書。** 複数の計画書を同一セッションで跨がない(文脈混線の防止)。
- **セッションは使い捨て、状態はファイルが持つ。** セッション開始時は必ず次から現在地を復元し、
  最初の応答で宣言してから作業に入る。記憶に頼らない:
  1. 対象計画書のヘッダ(版数)と進捗(未着手 / ST-x まで完了)
  2. `git log --oneline -10`(ブランチ 2026refactor)
  3. `CHECKSUMS.md` / `findings.md`
- 計画書の項目は**1項目=1コミット**。完了条件をコマンドで満たしてからコミット、
  満たせなければ「戻し方」で破棄して報告・停止。

## 計画書の所在(ルーティング)

| 仕事 | 計画書 | 対象 |
|---|---|---|
| Phase 1 リファクタリング(**完遂済み**・タグ refactor-v1-complete) | `refactor_plan.md`(参照のみ) | simplicity |
| スタイル基盤刷新(**進行中**) | `style_plan.md`(版数はヘッダが正。正本はこの1箇所) | simplicity のみ(quantz-web は読み取り抽出のみ・ST-R まで不触) |

計画書に無い作業を頼まれたら「どの計画の管轄か / 新しい計画が要るか」を先に答え、
勝手に着手しない。

## ブランチ・push 規約

- 作業ブランチは `2026refactor`(オーナー裁定 2026-07-06 の統一名)。
- `refactor/2026` ブランチと完遂タグは**不変アンカー**(D-14 / D-24)。改名・付け替え・削除を
  しない。成果物不変なら新タグの追加も不要。この確認をユーザーに再度求めない。
- 各項目のコミット後 `git push -u origin 2026refactor`。push 失敗(認証等)は作業を止めず、
  失敗の事実を報告して次項目へ進んでよい(コミットはローカルに残る)。force push は禁止。

## 権限境界(厳守)

- `.claude/settings.json` が強制する。**settings 自体を書き換えない。**
- 計画書(`*_plan.md`)は読み取り専用。実行者が自分の指示書を書き換えることは許されない。
  計画に問題を見つけたら findings.md に記録して報告する。
- `docs/coding_guides/` は規範。**実行者による書き換えを禁ずる**(規範化・改訂は人間のみ)。
- **quantz-web リポジトリに書き込まない**(ブランチ作成・コミット・push を含む一切。
  style_plan §4 / ST-R 参照)。読み取りは ST-0 / ST-2 の抽出時のみ。

## リモート前提の振る舞い

ユーザーはスマホ(Remote Control)から監視・承認することがある。
- 実行結果(テスト出力、ビルドエラー、diff、sha)は**丸めずに**出す。「省略」「割愛」禁止。
- 承認が要る操作はコマンドを提示して待つ。承認プロンプトはスマホ側にも出る。
- ブラウザ確認が必要な場面は URL を明示し、確認結果を尋ねる。

## 環境の注意

- Node は v18+、Python(プレビューランチャ venv)は /opt/homebrew/bin/python3.11(arm64)。
  依存はすべて exact ピン。
- **オーナーのシェルは zsh。手渡しする貼り付け用コマンドにインラインコメントを入れない。**
  zsh の対話シェルは行頭 `#` をコメント扱いしない(`interactivecomments` 既定オフ)ため、
  paste-ready ブロックに `# ...` を混ぜると `parse error` でブロック全体が死ぬ。
  説明はコマンドブロックの外(散文)に書く(2026-07-15 実測)。
- **パスは明示する(D-21)。** ツール・ランタイムに渡すパスは暗黙探索・暗黙 glob に依存せず
  明示列挙する(例: `node --test 'test/**/*.test.js'`)。計画と実環境が食い違ったら D-21 の
  修復手順(findings 記録 → 挙動不変の最小置換の提案 → 要所は承認待ち)。推測で進まない。

## 文書の優先順位(矛盾時の解決規則)

1. **計画書(`style_plan.md`)** — 実行の唯一の規範
2. CLAUDE.md(長期規約)+ 本ファイル
3. 並置された monorepo の `../thinkx-system/docs/ROADMAP.md` / `docs/DECISIONS.md` —
   全体順序と確定済み決定の典拠(変更は人間のみ。並置が無い環境では参照省略可・findings に記録)
4. **コーディング規約**(CLAUDE.md の探索順に従う。読まずに書くことを禁ずる)
5. 過去の設計議論(`../thinkx-system/docs/archive/`)— 参考・非規範。撤回済み提案を含むため、
   ここを根拠に作業方針を変えることを禁ずる。

上位と下位が食い違ったら常に上位に従い、食い違いの事実を findings.md に記録する。

## コード規約の探索(規範)

コードを書く/変更する前に、以下の順で規約を探し、**見つかったものは全て適用する**:
1. 本リポジトリ `docs/coding_guides/`(coding_principles_2022 + status — 効き方は CLAUDE.md 参照)
2. 並置 `../thinkx-system/docs/coding_guides/`(thinkx_coding_axioms.md・thinkx_coding_guide.md・
   言語別。並置が無ければ省略し findings に記録)
3. CLAUDE.md の固有規約節

規約に反する既存コードを見つけても**勝手に直さない**(findings に記録)。規約が存在しない
言語・ツールで繰り返し同じ失敗をした場合は、規約の新設を findings で**提案**する
(自分で規約を作らない。規範化は人間のみ)。